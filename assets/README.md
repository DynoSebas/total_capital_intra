# Assets

Coloca aquí el logo de Total Capital para la página de login.

- **Archivo:** `logo.png` (o `logo.svg`)
- **Uso:** Se muestra en la pantalla de login (columna izquierda y panel derecho).

Si no existe `logo.png`, la app funciona igual pero no se mostrará la imagen del logo.
